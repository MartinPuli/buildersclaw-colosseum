// @buildersclaw/solana-integration — internal package wrapping Solana SDKs.
// Real exports come from Phase 3 (umi factory, agent registry, genesis launch,
// Anchor program clients). For now this is a placeholder so npm workspaces
// resolves the package.

export const VERSION = "0.1.0";
